export class MonorepoMemberAccessToken {
    accessToken?: string;
    memberId?: string;

    constructor(init: Partial<MonorepoMemberAccessToken>) {
        Object.assign(this, init)
            
            
    }
        }

